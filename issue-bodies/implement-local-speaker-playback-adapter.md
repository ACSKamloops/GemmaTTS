# Implement local speaker playback adapter

## Summary
Allow the service to play generated audio on the local machine for desktop assistant use.

## Acceptance criteria
- [ ] Adapter supports blocking and non-blocking playback.
- [ ] Playback can be disabled in server mode.
- [ ] Device selection is configurable where supported.
